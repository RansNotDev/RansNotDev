import { buildPortfolioContext } from './portfolioKnowledge'

/** Canonical, employer-neutral grounding context for the portfolio assistant. */
export const PORTFOLIO_CONTEXT = buildPortfolioContext()
